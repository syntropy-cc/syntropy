# Dockerfile.dev
FROM node:20-alpine

WORKDIR /app

# Enable Corepack for proper package manager support
RUN corepack enable

# Install dependencies
COPY package*.json ./

# Use the project's specified package manager or fallback to npm
RUN if [ -f package-lock.json ]; then \
        npm ci; \
    elif [ -f yarn.lock ]; then \
        yarn install; \
    elif [ -f pnpm-lock.yaml ]; then \
        pnpm install; \
    else \
        npm install; \
    fi

# Copy source code (será sobrescrito pelo volume mount)
COPY . .

# Expose port
EXPOSE 3000

# Start development server with hot reload
CMD ["npm", "run", "dev"]